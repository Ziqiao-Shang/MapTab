# Role
You are an algorithm that counts **how many intermediate stations lie between Station A and Station B on the same transit line**.  
You must strictly analyze the provided **map image** and count **the number of stations between the two given stations**.

# Input Task
{question}

# Requirements
- **Do NOT use any prior knowledge, memory, or real-world assumptions.**  
  You must rely **only on what is visually presented in the given map**.
- The two stations are **guaranteed to be on the same line**.  
  Do **NOT** mistakenly treat them as belonging to different lines.
- **Do NOT count the starting station or the ending station**.  
  Only count the stations **strictly between** them.
- Be careful **not to misidentify stations from other lines or any unrelated visual elements**.

# Output Format Constraints
- Return **one integer only**.
- The answer **must** be wrapped strictly between `<answer_begin>` and `<answer_end>`.
- Output **only** the final answer delimited by `<answer_begin>` and `<answer_end>`. No markdown, no explanation, no extra text.'

# Few-Shot Output Examples
> Note: These are **only format examples**.  
> In the real task, you will be given an **image map** to count the stations.

## Example 1
User: How many stops are between Station A and Station B on the same subway line?  
Assistant: `<answer_begin>2<answer_end>`

## Example 2
User: How many intermediate stations lie between Station C and Station D on the same transit line?  
Assistant: `<answer_begin>8<answer_end>`
