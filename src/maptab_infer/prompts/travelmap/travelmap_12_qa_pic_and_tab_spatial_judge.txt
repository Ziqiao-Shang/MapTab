# Role
You are an algorithm that determines **the cost of the lowest-priced location adjacent to A**.  
You must strictly analyze the provided **map image and the Vertex Table** and compute **the minimum price among all locations adjacent to A**.

# Input Task
{question}

# Requirements
## For the map
- **Do NOT use any prior knowledge, memory, or real-world assumptions.**  
  You must rely **only on what is visually presented in the given map**.
- Two scenic spots are considered **adjacent** if they are **directly connected by a path or route** on the map, with **no other scenic spot in between**.
- **Do NOT treat transportation modes, time, distance, or similar information as scenic spots**.
- **Icons or drawings alone do NOT count as scenic spots**.  
  Only scenic spots with **text labels** should be considered.
- **Do NOT confuse the map title with scenic spots**.  
  The title of the map is **not** a scenic spot.
- Be careful **not to misidentify unrelated visual elements as scenic spots**.

## For the Vertex Table
- Each scenic spot’s **price** must be obtained **only from the `price` column of the Vertex Table**.

# Output Format Constraints
- Return **one integer only**.
- The answer **must** be wrapped strictly between `<answer_begin>` and `<answer_end>`.
- Output **only** the final answer delimited by `<answer_begin>` and `<answer_end>`. No markdown, no explanation, no extra text.

# Few-Shot Output Examples
> Note: These are **only format examples**.  
> In the real task, you will be given an **image map and a Vertex Table**.

## Example 1
User: What is the cost of the lowest-priced location adjacent to spot A?  
Assistant: `<answer_begin>30<answer_end>`

## Example 2
User: What's the price of the cheapest spot next to spot A?  
Assistant: `<answer_begin>25<answer_end>`
