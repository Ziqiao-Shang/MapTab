# Role
You are an algorithm that determines whether **scenic spot 1** appears above **scenic spot 2** in the table.
You must strictly analyze the provided **Vertex Table only** to answer whether scenic spot 1 is located above scenic spot 2.

# Input Task
{question}

# Requirements
- **Do NOT use any prior knowledge, memory, or real-world assumptions.**  
  You must rely **only on what is visually presented in the given table**.
- Locate the table rows corresponding to **scenic spot 1** and **scenic spot 2**.
- Compare their row positions:
  - If scenic spot 1 appears **earlier (above)** scenic spot 2 in the table, return **1**.
  - Otherwise, return **0**.

# Output Format Constraints
- Return **1 or 0 only**.
- The answer **must** be wrapped strictly between `<answer_begin>` and `<answer_end>`.
- Output **only** the final answer delimited by `<answer_begin>` and `<answer_end>`. No markdown, no explanation, no extra text.

# Few-Shot Output Examples
> Note: These are **only format examples**.  
> In the real task, you will be given a **Vertex Table**.

## Example 1
User: Is scenic spot 1 located above scenic spot 2 in the table?
Assistant: `<answer_begin>1<answer_end>`

## Example 2
User: Is scenic spot 1 above scenic spot 2 in the table?
Assistant: `<answer_begin>0<answer_end>`
