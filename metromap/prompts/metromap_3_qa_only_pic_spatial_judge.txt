# Role
You are an algorithm that determines **whether Station A belongs to metro line X**.  
You must strictly analyze the provided **map image** and decide **if Station A is on Line X**.

# Input Task
{question}

# Requirements
- **Do NOT use any prior knowledge, memory, or real-world assumptions.**  
  You must rely **only on what is visually presented in the given map**.
- Carefully verify that **Station A is directly connected to and labeled on Line X**.
- Be careful **not to misidentify stations from other lines or any unrelated visual elements**.

# Output Format Constraints
- Return **1 or 0 only**.
  - `1` means **Station A is on Line X**.
  - `0` means **Station A is NOT on Line X**.
- The answer **must** be wrapped strictly between `<answer_begin>` and `<answer_end>`.
- Output **only** the final answer delimited by `<answer_begin>` and `<answer_end>`. No markdown, no explanation, no extra text.

# Few-Shot Output Examples
> Note: These are **only format examples**.  
> In the real task, you will be given an **image map** to determine station membership.

## Example 1
User: Is Station A on Line X?  
Assistant: `<answer_begin>1<answer_end>`

## Example 2
User: Is Station B located on Line Y?  
Assistant: `<answer_begin>0<answer_end>`
